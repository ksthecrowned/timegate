# Base URL de l'API TimeGate (obligatoire sur appareil physique : IP LAN, pas localhost).
# Émulateur Android : http://10.0.2.2:4001/api/v1
# Même sous-réseau Wi-Fi que le PC qui lance Metro (ipconfig / ifconfig).
EXPO_PUBLIC_TIMEGATE_API_URL=http://192.168.1.10:4001/api/v1
