EXPO_PUBLIC_TIMEGATE_API_URL=http://172.20.10.2:4000/api
# The first launch setup screen asks for:
# - admin/manager email/password
# - site UUID (if creating a new device)
# - optional device UUID OR a new device name
