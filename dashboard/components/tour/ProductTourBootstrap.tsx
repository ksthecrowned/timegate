'use client'

/**
 * Auto-start bootstrap — intentionally disabled.
 * Tour starts only via « Start tour » (manual) or TourResumeModal.
 * Re-enable by mounting this component in AuthenticatedShell when ready.
 */
export default function ProductTourBootstrap() {
  return null
}
